import { ConfigureButtonPipe } from './configure-button.pipe';

describe('ConfigureButtonPipe', () => {
  it('create an instance', () => {
    const pipe = new ConfigureButtonPipe();
    expect(pipe).toBeTruthy();
  });
});
